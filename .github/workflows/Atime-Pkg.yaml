on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: Atime-Pkg

jobs:
  Atime-Pkg:
    runs-on: ubuntu-latest
    steps:
      - uses: SiyaoIsHiding/R_GA_GSOC@v0.0.67
        with:
          alpha: '"0.1"'
