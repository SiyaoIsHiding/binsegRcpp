on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: Atime-Pkg

jobs:
  Atime-Pkg:
    runs-on: ubuntu-latest
    steps:
      - uses: SiyaoIsHiding/r-asymptotic-testing@v0.0.2
        with:
          alpha: '"0.5"'
